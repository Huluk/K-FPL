"call by value:";
function square_value (x) {
    * x x
};

"call by name:";
sub square_name (x) {
    * x x
};

"call by need:";
def square_need (x) {
    * x x
};

let x = (5);
let y = (5);
let z = (5);

[square_value(++x),
 square_name(++y),
 square_need(++z)]
